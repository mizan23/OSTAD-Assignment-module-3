name: Node.js CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: Test Job
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Run tests and save results
        run: |
          npm run check > test-results.txt || true

      - name: Upload test results artifact
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results.txt

  deploy:
    name: Deploy Job
    needs: test
    runs-on: [self-hosted, node-runner]

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      # 1️⃣ Install Node.js 22 PERMANENTLY
      - name: Install Node.js 22 system-wide
        run: |
          if ! command -v node >/dev/null 2>&1; then
            curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
            sudo apt-get install -y nodejs
          fi

      - name: Verify Node and npm
        run: |
          node -v
          npm -v

      # 2️⃣ Install PM2 PERMANENTLY
      - name: Install PM2 system-wide
        run: |
          if ! command -v pm2 >/dev/null 2>&1; then
            sudo npm install -g pm2
          fi

      # 3️⃣ Ensure PM2 starts on reboot
      - name: Configure PM2 startup
        run: |
          pm2 startup systemd -u ubuntu --hp /home/ubuntu || true

      # 4️⃣ Get test artifact
      - name: Download test results artifact
        uses: actions/download-artifact@v4
        with:
          name: test-results

      - name: Display test results
        run: cat test-results.txt

      # 5️⃣ Install app dependencies
      - name: Install application dependencies
        run: npm install

      # 6️⃣ DEPLOY AND MAKE IT STAY RUNNING
      - name: Deploy application and persist
        run: |
          echo "Deploying Node.js application..."
          pm2 delete node-app || true
          pm2 start "./src/server.js" --name node-app
          pm2 save
          pm2 list

